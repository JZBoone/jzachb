extends layout

block content
  h1= page.title
  p Each link takes you to a page that lists the technologies used, the inspiration behind the demo, and a brief explanation of my thought process.
  - for (let d of demos)
      a(
        href = `/demos/blurbs/${d.path}`,
      ) #{d.title}
      span &nbsp;- #{d.description}
      br
      br
